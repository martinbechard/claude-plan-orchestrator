meta:
  source_item: "tmp/plans/.claimed/68-design-competition-auto-judge.md"
  name: "Design competition auto-judge"
  description: "Create a design-judge agent and wire it into the planner and plan creation prompt so design competitions auto-judge without blocking for human input"
  plan_doc: "docs/plans/2026-03-27-68-design-competition-auto-judge-design.md"
  validation:
    enabled: true
    run_after:
      - coder
    validators:
      - validator
    max_validation_attempts: 2

sections:
  - id: "1"
    name: "Create design-judge agent and update planner"
    status: "pending"
    tasks:
      - id: "1.1"
        name: "Create design-judge agent and wire into planner and plan creation"
        agent: "coder"
        status: "pending"
        description: "Implement the work item: tmp/plans/.claimed/68-design-competition-auto-judge.md — Create .claude/agents/design-judge.md (Opus agent that reads competing designs, evaluates against acceptance criteria, produces scoring matrix, selects winner with rationale, writes judgment to worker output, and completes without suspending). Update .claude/agents/planner.md to reference design-judge for Phase 0 judge tasks. Update PLAN_CREATION_PROMPT in langgraph_pipeline/pipeline/nodes/plan_creation.py to include design-judge in the available agents list. See design: docs/plans/2026-03-27-68-design-competition-auto-judge-design.md"
