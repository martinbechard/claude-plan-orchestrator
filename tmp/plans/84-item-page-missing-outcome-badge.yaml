meta:
  name: 84-item-page-missing-outcome-badge
  source_item: tmp/plans/.claimed/84-item-page-missing-outcome-badge.md
  requirements_path: docs/plans/2026-04-02-84-item-page-missing-outcome-badge-requirements.md
  validation:
    enabled: true
    run_after:
    - coder
    - e2e-test-agent
    - frontend-coder
    validators:
    - validator
    max_validation_attempts: 2
sections:
- id: '1'
  name: 'Implementation: Outcome Badge Backend and Frontend'
  status: pending
  tasks:
  - id: '1.1'
    name: Add outcome derivation, template badge, CSS, and dynamic-refresh support
    agent: frontend-coder
    status: completed
    target_acs:
    - AC1
    - AC2
    - AC3
    - AC4
    - AC5
    - AC6
    - AC7
    - AC8
    description: 'Implement P1, P2, UC1, FR1, FR2 per D1, D2, D3, D4, D5 from work
      item: tmp/plans/.claimed/84-item-page-missing-outcome-badge.md Design: docs/plans/2026-04-02-84-item-page-missing-outcome-badge-design.md
      Requirements: docs/plans/2026-04-02-84-item-page-missing-outcome-badge-requirements.md

      In langgraph_pipeline/web/routes/item.py: D1: Add _derive_outcome(completions:
      list[dict]) -> Optional[str] that returns the outcome from the most recent completion
      record (highest finished_at), or None if completions is empty. D2: In item_detail(),
      call _derive_outcome(completions) and add "outcome" to the template context.
      In item_dynamic(), call _derive_outcome(completions) and add "outcome" to the
      JSON response.

      In langgraph_pipeline/web/templates/item.html: D3: Inside the item-badges div,
      insert a new outcome badge BEFORE the item_type badge. Use data-dynamic="outcome-badge"
      wrapper. Render only when outcome is not None. Badge text is the outcome value.
      Add CSS class outcome-{{ outcome }}. D4: Add CSS classes: outcome-success (green),
      outcome-warn (amber), outcome-fail (red). Use font-weight 700 for prominence
      (vs 600 for standard badges) to make the outcome badge visually distinguishable
      (AC7) and more prominent (AC8). D5: In the selective-refresh JS, add a handler
      for data.outcome that updates the outcome-badge element.

      Write tests in tests/langgraph/web/test_item_outcome_badge.py covering: _derive_outcome
      returns latest completion outcome, returns None for empty list, handles multiple
      completions correctly, and handles all three outcome values.

      '
    last_attempt: '2026-04-02T14:31:49.355957'
    attempts: 1
    model_used: sonnet
    completed_at: '2026-04-02T14:33:41.822278'
    result_message: 'All deliverables (D1-D5) were already implemented: _derive_outcome()
      helper in item.py, outcome context in item_detail() and item_dynamic(), outcome
      badge markup/CSS in item.html, and JS dynamic-refresh handler. Test file test_item_outcome_badge.py
      exists with 10 tests. All 10 target tests and full suite (1764 tests) pass.'
