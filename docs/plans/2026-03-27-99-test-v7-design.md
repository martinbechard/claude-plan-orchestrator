# Design: 99-test-v7 - Pipeline with tmp/plans and Token Tracking

## Summary

Regression test validating two recent changes:
1. Plans directory refactoring from `.claude/plans/` to `tmp/plans/`
2. Token tracking instrumentation captures LLM usage through completed plans

## Change

Add a comment `# tmp plans test` to `langgraph_pipeline/shared/paths.py`.

## Key Files

- **Modified**: `langgraph_pipeline/shared/paths.py` - add comment line

## Acceptance Criteria

1. `langgraph_pipeline/shared/paths.py` contains `# tmp plans test`
2. Plan YAML created without permission denials
3. Item page shows non-zero Tokens after completion

## Design Decisions

- Single task: the change is a one-line comment addition
- Uses coder agent since it modifies a Python source file
- Validation confirms the comment exists in the file after task execution
