meta:
  source_item: .claude/plans/.claimed/02-traces-runs-named-langgraph.md
  validation:
    enabled: true
    run_after:
    - coder
    - e2e-test-agent
    - frontend-coder
    validators:
    - validator
    max_validation_attempts: 2
sections:
- id: '1'
  name: Fix root trace naming
  status: pending
  tasks:
  - id: '1.1'
    name: Fix finalize_root_run and cli.py run_name
    agent: coder
    status: pending
    description: 'Implement the work item: .claude/plans/.claimed/02-traces-runs-named-langgraph.md

      Fix all three root causes identified in the design doc (docs/plans/2026-03-26-02-traces-runs-named-langgraph-design.md):
      (1) finalize_root_run in langsmith.py — accept item_slug param and use it as
      RunTree name, (2) cli.py — add run_name to thread_config in single-item, once,
      and loop modes, (3) execute_plan.py — pass run_name config to executor invoke.
      Update existing tests or add new ones for finalize_root_run.'
