meta:
  name: 'Defect Fix: Self-skip filter drops legitimate messages'
  description: Replace identity-based self-skip filters (bot_id blanket skip and signature
    pattern skip) with processed-message deduplication and rate-based loop detection.
    Allows legitimate bot-originated messages through while preventing infinite self-reply
    loops.
  plan_doc: docs/plans/2026-02-24-01-self-skip-filter-drops-legitimate-messages-design.md
  source_item: docs/defect-backlog/01-self-skip-filter-drops-legitimate-messages.md
  created: '2026-02-24'
  max_attempts_default: 3
  validation:
    enabled: true
    run_after:
    - coder
    - e2e-test-agent
    - frontend-coder
    validators:
    - validator
    max_validation_attempts: 2
  status: failed
  failure_reason: 'Deadlock: 2.1 blocked by [''1.1'']'
sections:
- id: phase-1
  name: Phase 1 - Replace identity filters with dedup and loop detection
  status: pending
  tasks:
  - id: '1.1'
    name: Replace bot_id and signature filters with dedup and loop detection
    agent: coder
    status: failed
    description: "Implement the work item: docs/defect-backlog/01-self-skip-filter-drops-legitimate-messages.md\n\
      Design doc: docs/plans/2026-02-24-01-self-skip-filter-drops-legitimate-messages-design.md\n\
      \nIn scripts/plan-orchestrator.py:\n\n1. Add constants near the top of the file:\n\
      \   MAX_SELF_REPLIES_PER_WINDOW = 3\n   LOOP_DETECTION_WINDOW_SECONDS = 60\n\
      \   MESSAGE_TRACKING_TTL_SECONDS = 3600\n\n2. In SlackNotifier.__init__(), add:\n\
      \   self._processed_message_ts: set[str] = set()\n   self._own_sent_ts: set[str]\
      \ = set()\n   self._self_reply_window: dict[str, list[float]] = {}\n\n3. In\
      \ _post_message(), parse ts from the response JSON and add it to\n   self._own_sent_ts.\
      \ In _post_message_get_ts(), also add the returned\n   ts to self._own_sent_ts.\n\
      \n4. In _handle_polled_messages(), replace Rule 0 (bot_id skip) and\n   Rule\
      \ 1 (signature skip) with:\n   a. Dedup: if msg ts in _processed_message_ts,\
      \ skip with log\n      \"[SLACK] Filter: skip already-processed ts=...\"\n \
      \  b. Self-origin + loop detection: if msg ts in _own_sent_ts,\n      count\
      \ recent entries in _self_reply_window[channel]. If count >=\n      MAX_SELF_REPLIES_PER_WINDOW,\
      \ skip with log\n      \"[SLACK] Filter: skip loop-detected\". Otherwise log\n\
      \      \"[SLACK] Filter: accept self-origin\" and continue.\n   c. After routing,\
      \ add ts to _processed_message_ts. If self-origin,\n      record time.monotonic()\
      \ in _self_reply_window[channel].\n   Keep Rules 2-4 (@AgentName addressing)\
      \ unchanged.\n\n5. Add _prune_message_tracking() method to clean old entries.\n\
      \   Call it at the start of each poll cycle in process_inbound().\n\n6. Verify\
      \ syntax: python3 -c \"import py_compile; py_compile.compile(\n   'scripts/plan-orchestrator.py',\
      \ doraise=True)\"\n\nFiles: scripts/plan-orchestrator.py\n"
    attempts: 3
    last_attempt: '2026-02-24T23:55:08.598218'
    model_used: sonnet
    last_error: Task timed out after 900 seconds
    validation_findings: 'Validator ''validator'' failed to execute: No status file
      written by Claude'
    validation_attempts: 1
- id: phase-2
  name: Phase 2 - Update tests
  status: pending
  tasks:
  - id: '2.1'
    name: Update unit tests for new dedup and loop detection logic
    agent: coder
    depends_on:
    - '1.1'
    status: pending
    description: "Implement the work item: docs/defect-backlog/01-self-skip-filter-drops-legitimate-messages.md\n\
      Design doc: docs/plans/2026-02-24-01-self-skip-filter-drops-legitimate-messages-design.md\n\
      \nUpdate tests in tests/test_slack_notifier.py and tests/test_agent_identity.py:\n\
      \n1. Remove or replace tests for bot_id blanket skip (Rule 0) and\n   signature-based\
      \ skip (Rule 1) since those filters no longer exist.\n\n2. Add tests for processed-message\
      \ deduplication:\n   - Same ts processed twice is skipped on second encounter\n\
      \   - Different ts values are both processed\n\n3. Add tests for sent-ts tracking:\n\
      \   - _post_message records ts in _own_sent_ts\n   - _post_message_get_ts records\
      \ ts in _own_sent_ts\n\n4. Add tests for self-origin message handling:\n   -\
      \ Message with ts in _own_sent_ts is processed when under rate limit\n   - Message\
      \ with ts in _own_sent_ts is skipped when loop detected\n     (exceeding MAX_SELF_REPLIES_PER_WINDOW\
      \ in window)\n\n5. Add tests for _prune_message_tracking cleanup:\n   - Old\
      \ entries are removed from _processed_message_ts\n   - Old entries are removed\
      \ from _own_sent_ts\n   - Old window entries are pruned from _self_reply_window\n\
      \n6. Ensure existing tests for Rules 2-4 (@AgentName addressing) still pass.\n\
      \nRun tests: ~/.pyenv/versions/3.11.*/bin/python -m pytest tests/ -v\nFix any\
      \ failures.\n\nFiles: tests/test_slack_notifier.py, tests/test_agent_identity.py\n"
