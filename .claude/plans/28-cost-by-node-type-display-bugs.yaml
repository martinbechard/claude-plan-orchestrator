meta:
  name: cost-by-node-type-display-bugs
  source_item: .claude/plans/.claimed/28-cost-by-node-type-display-bugs.md
  design: docs/plans/2026-03-26-28-cost-by-node-type-display-bugs-design.md
  validation:
    enabled: true
    run_after:
    - coder
    - e2e-test-agent
    - frontend-coder
    validators:
    - validator
    max_validation_attempts: 2
sections:
- id: '1'
  name: Fix svg_bar_chart value label clipping and float formatting
  status: pending
  tasks:
  - id: '1.1'
    name: Fix SVG_VALUE_LABEL_PADDING and float value formatting in svg_bar_chart
    agent: coder
    status: pending
    description: 'Implement the work item: .claude/plans/.claimed/28-cost-by-node-type-display-bugs.md
      -- fix both bugs in svg_bar_chart() in langgraph_pipeline/web/cost_log_reader.py:
      (1) increase SVG_VALUE_LABEL_PADDING from 10 to 80 so value labels are not
      clipped; (2) change values parameter type from list[int] to list[float] and
      format value labels as f"${value:.2f}" instead of {value:,}. Update
      tests/langgraph/web/test_cost_log_reader.py to pass float values and assert
      the "$"-prefixed formatted output. Run the full test suite and fix any
      failures.'
