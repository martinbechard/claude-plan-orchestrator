meta:
  source_item: .claude/plans/.claimed/04-timeline-duplicate-labels-and-elapsed-time.md
  item_type: defect
  validation:
    enabled: true
    run_after:
    - coder
    - frontend-coder
    validators:
    - validator
    max_validation_attempts: 2
sections:
- id: '1'
  name: Route and Template Fix
  status: pending
  tasks:
  - id: '1.1'
    name: Compute elapsed times and pass span_s from route
    agent: coder
    description: 'Implement the work item: .claude/plans/.claimed/04-timeline-duplicate-labels-and-elapsed-time.md

      Focus on the Python route changes in langgraph_pipeline/web/routes/proxy.py.
      In proxy_trace(), after fetching children, compute elapsed_start_s and elapsed_end_s
      per child using datetime subtraction from the root run start_time. Also compute
      span_s as a float covering all children. Batch-fetch grandchild counts using
      count_children_batch. Pass span_s and grandchild_counts to the template context.
      See the design doc at docs/plans/2026-03-26-04-timeline-duplicate-labels-and-elapsed-time-design.md.'
    status: in_progress
    last_attempt: '2026-03-26T01:19:16.627916'
    attempts: 1
    model_used: sonnet
  - id: '1.2'
    name: Fix Gantt chart template tick labels and bar positioning
    agent: frontend-coder
    description: 'Implement the work item: .claude/plans/.claimed/04-timeline-duplicate-labels-and-elapsed-time.md

      Focus on langgraph_pipeline/web/templates/proxy_trace.html. Replace the secs()
      macro with the pre-computed elapsed_start_s / elapsed_end_s fields from the
      route. Use float division (/) instead of integer // for bar x/width and tick
      positions. Format axis tick labels as elapsed offsets (+Xs or +Xm Ys). Add a
      grandchildren expandable section below the Gantt chart using HTML details/summary
      for children with grandchild_count > 0. See the design doc at docs/plans/2026-03-26-04-timeline-duplicate-labels-and-elapsed-time-design.md.'
    status: pending
