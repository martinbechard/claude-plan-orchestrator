meta:
  source_item: .claude/plans/.claimed/03-dashboard-items-stuck-running.md
  item_type: defect
  validation:
    enabled: true
    run_after:
    - coder
    validators:
    - validator
    max_validation_attempts: 2
sections:
- id: '1'
  name: Fix dead-worker cleanup in DashboardState and supervisor
  status: pending
  tasks:
  - id: '1.1'
    name: Add sweep_dead_workers() to DashboardState and fix ChildProcessError reap
      path
    agent: coder
    description: 'Implement the fix described in the work item and design doc: .claude/plans/.claimed/03-dashboard-items-stuck-running.md


      Design doc: docs/plans/2026-03-26-03-dashboard-items-stuck-running-design.md


      Two changes required:


      1. In langgraph_pipeline/web/dashboard_state.py: add a sweep_dead_workers()
      method to DashboardState that iterates active_workers, uses os.kill(pid, 0)
      to test liveness, and calls remove_active_worker(pid, "fail", 0.0, elapsed_s)
      for dead PIDs. Call sweep_dead_workers() at the top of snapshot() before building
      the active_list.


      2. In langgraph_pipeline/supervisor.py: in _reap_finished_workers(), when ChildProcessError
      is caught, pop the record from active_workers, compute elapsed_s from the stored
      start_time in the record, and call get_dashboard_state().remove_active_worker(pid,
      "fail", 0.0, elapsed_s) before continuing.


      Run the full test suite and fix any failures.'
    status: pending
