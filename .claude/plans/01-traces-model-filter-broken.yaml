meta:
  source_item: .claude/plans/.claimed/01-traces-model-filter-broken.md
  item_type: defect
  validation:
    enabled: true
    run_after:
    - coder
    validators:
    - validator
    max_validation_attempts: 2
sections:
- id: '1'
  name: Fix model storage and filter
  status: pending
  tasks:
  - id: '1.1'
    name: Add model column and propagation to TracingProxy
    agent: coder
    description: 'Implement the work item: .claude/plans/.claimed/01-traces-model-filter-broken.md


      Read the design first: docs/plans/2026-03-26-01-traces-model-filter-broken-design.md


      Focus on Task 1.1: In langgraph_pipeline/web/proxy.py, add a model TEXT NOT
      NULL DEFAULT '''''' column to _CREATE_TABLE_SQL. In _init_db(), after creating
      the table, run ALTER TABLE traces ADD COLUMN model TEXT NOT NULL DEFAULT ''''
      and suppress the OperationalError if the column already exists. Add an index
      idx_traces_model. Add a propagate_model_to_root(parent_run_id, model) method
      that walks up the parent chain and sets model on the root run only when model
      is currently empty. Update list_runs() and count_runs() to filter with LOWER(model)
      LIKE LOWER(?) instead of metadata_json LIKE ?.'
    status: in_progress
    last_attempt: '2026-03-26T01:08:48.485990'
    attempts: 1
    model_used: sonnet
  - id: '1.2'
    name: Extract model in server.py and fix enrich_run display
    agent: coder
    description: 'Implement the work item: .claude/plans/.claimed/01-traces-model-filter-broken.md


      Read the design first: docs/plans/2026-03-26-01-traces-model-filter-broken-design.md


      Focus on Task 1.2: In langgraph_pipeline/web/server.py, in both runs_multipart
      and runs_create, after calling proxy.record_run(), extract the model string
      from extra.invocation_params (keys: model, model_name) and extra.metadata (keys:
      ls_model_name, model). If a model string is found and parent_run_id is non-empty,
      call proxy.propagate_model_to_root(parent_run_id, model). In langgraph_pipeline/web/routes/proxy.py,
      update _enrich_run() to set display_model from run.get("model", "") instead
      of parsing metadata_json. Update tests in tests/langgraph/web/test_proxy.py:
      add tests for propagate_model_to_root(), model filter in list_runs()/count_runs(),
      and multipart model extraction via the /runs/multipart endpoint.'
    status: pending
