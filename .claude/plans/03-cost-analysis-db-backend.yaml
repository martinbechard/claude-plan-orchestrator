meta:
  source_item: .claude/plans/.claimed/03-cost-analysis-db-backend.md
  item_type: feature
  validation:
    enabled: true
    run_after:
    - coder
    validators:
    - validator
    max_validation_attempts: 2
sections:
- id: '1'
  name: DB Schema and API Endpoint
  status: pending
  tasks:
  - id: '1.1'
    name: Add cost_tasks table to SQLite DB
    agent: coder
    description: 'Implement the work item: .claude/plans/.claimed/03-cost-analysis-db-backend.md


      Focus on Section 1, Task 1.1: Add the cost_tasks table to the existing SQLite
      DB. In langgraph_pipeline/web/proxy.py, add _CREATE_COST_TASKS_SQL with the
      schema from the work item and call conn.execute(_CREATE_COST_TASKS_SQL) in _init_db().
      Add the corresponding index on item_slug.'
    status: completed
    last_attempt: '2026-03-26T01:03:09.273897'
    attempts: 1
    model_used: sonnet
    completed_at: '2026-03-26T01:04:35.656176'
    result_message: Added _CREATE_COST_TASKS_SQL schema and idx_cost_tasks_item_slug
      index to proxy.py; _init_db() now creates the cost_tasks table alongside traces
      and completions. All 1494 tests pass.
    validation_attempts: 1
    validation_findings: cost_tasks table schema matches spec exactly; idx_cost_tasks_item_slug
      index added; _init_db() calls conn.execute(_CREATE_COST_TASKS_SQL); build OK;
      1494/1494 tests pass.
  - id: '1.2'
    name: Add POST /api/cost endpoint
    agent: coder
    description: 'Implement the work item: .claude/plans/.claimed/03-cost-analysis-db-backend.md


      Focus on Section 1, Task 1.2: Create langgraph_pipeline/web/routes/cost.py with
      a FastAPI router implementing POST /api/cost. Accept the JSON payload from the
      work item, insert a row into cost_tasks via the TracingProxy DB connection,
      return {"ok": true} with status 202. Register the router in langgraph_pipeline/web/server.py
      create_app(). Write unit tests in tests/langgraph/web/test_cost_endpoint.py
      using FastAPI TestClient.'
    status: completed
    last_attempt: '2026-03-26T01:06:07.155842'
    attempts: 1
    model_used: sonnet
    completed_at: '2026-03-26T01:09:23.928872'
    result_message: 'PASS: All good'
    validation_attempts: 1
    validation_findings: 'PASS: All good'
- id: '2'
  name: Worker and Reader Updates
  status: pending
  tasks:
  - id: '2.1'
    name: Update plan-orchestrator.py to POST cost data
    agent: coder
    description: 'Implement the work item: .claude/plans/.claimed/03-cost-analysis-db-backend.md


      Focus on Section 2, Task 2.1: Update write_execution_cost_log() in scripts/plan-orchestrator.py.
      When os.environ.get("LANGCHAIN_ENDPOINT", "") starts with "http://localhost",
      POST the per-task cost payload to {LANGCHAIN_ENDPOINT}/api/cost using urllib.request
      (no new dependencies). Keep existing JSON file write as the fallback. Handle
      HTTP errors and timeouts gracefully (log and continue).'
    status: completed
    last_attempt: '2026-03-26T01:11:04.345037'
    attempts: 1
    model_used: sonnet
    completed_at: '2026-03-26T01:12:51.805707'
    result_message: Added _post_cost_to_db() helper and updated write_execution_cost_log()
      to POST to LANGCHAIN_ENDPOINT/api/cost when endpoint starts with http://localhost,
      with graceful fallback to JSON file on failure. All 1506 tests pass.
    validation_attempts: 1
    validation_findings: 'Build passes, all 1506 tests pass, requirements met. One
      coding standards violation: COST_API_TIMEOUT_S constant defined inside function
      body (plan-orchestrator.py:970) instead of at module scope.'
  - id: '2.2'
    name: Update CostLogReader to query DB
    agent: coder
    description: 'Implement the work item: .claude/plans/.claimed/03-cost-analysis-db-backend.md


      Focus on Section 2, Task 2.2: Update langgraph_pipeline/web/cost_log_reader.py.
      Add a _load_from_db() method that queries cost_tasks grouped by item_slug using
      DB_DEFAULT_PATH from langgraph_pipeline/web/proxy.py. load_all() tries DB first;
      falls back to JSON file glob if DB does not exist or cost_tasks table is absent.
      Deserialise tool_calls_json column for _accumulate_read_bytes(). Update tests
      in tests/langgraph/web/test_cost_log_reader.py.'
    status: completed
    last_attempt: '2026-03-26T01:14:55.345839'
    attempts: 1
    model_used: sonnet
    completed_at: '2026-03-26T01:19:49.524216'
    result_message: 'Added _try_load_from_db() to CostLogReader: queries cost_tasks
      table grouped by item_slug using DB_DEFAULT_PATH, deserialises tool_calls_json
      for read-bytes accumulation. load_all() tries DB first, falls back to JSON file
      glob if DB absent or table missing. Refactored into _load_from_json() + _aggregate()
      helpers. Added 5 new DB-path tests; all 1517 tests pass.'
